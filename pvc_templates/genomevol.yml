apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: genomevol
  namespace: ecdna
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi