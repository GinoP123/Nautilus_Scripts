apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: bafnavol2-snapshot
  namespace: ecdna
spec:
  volumeSnapshotClassName: rook-cephfs
  source:
    persistentVolumeClaimName: bafnavol2
