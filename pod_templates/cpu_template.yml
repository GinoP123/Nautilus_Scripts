apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpu_POD_NUM
  namespace: ecdna
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: ecpod_POD_NUM
  template:
    metadata:
      labels:
        k8s-app: ecpod_POD_NUM
    spec:
      containers:
      - name: ecdna
        image: gitlab-registry.nrp-nautilus.io/prp/jupyter-stack/tensorflow:latest
        command: ["sleep", "infinity"]
        resources:
          limits:
            cpu: 3
            memory: "60Gi"
          requests:
            memory: "50Gi"
            cpu: 2
        volumeMounts:
        - mountPath: /home/bafnavol2
          name: bafnavol2
        securityContext:
          runAsUser: 0
          windowsOptions:
            runAsUserName: "giprasad"
      restartPolicy: Always
      volumes:
        - name: bafnavol2
          persistentVolumeClaim:
            claimName: bafnavol2
            readOnly: false