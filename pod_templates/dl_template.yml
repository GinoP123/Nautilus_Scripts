apiVersion: apps/v1
kind: Deployment
metadata:
  name: dl_POD_NUM
  namespace: ecdna
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-app: ecpod_POD_NUM
  template:
    metadata:
      labels:
        k8s-app: ecpod_POD_NUM
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: nvidia.com/gpu.product
                operator: In
                values:
                - NVIDIA-L40
      containers:
      - name: ecdna
        image: gitlab-registry.nrp-nautilus.io/prp/jupyter-stack/tensorflow:latest
        command: ["sleep", "infinity"]
        resources:
          limits:
            cpu: 3
            nvidia.com/gpu: 1
            memory: "60Gi"
          requests:
            memory: "50Gi"
            cpu: 2
        volumeMounts:
        - mountPath: /home/bafnavol
          name: bafnavol
        securityContext:
          runAsUser: 0
          windowsOptions:
            runAsUserName: "giprasad"
      restartPolicy: Always
      volumes:
        - name: bafnavol
          persistentVolumeClaim:
            claimName: bafnavol
            readOnly: false